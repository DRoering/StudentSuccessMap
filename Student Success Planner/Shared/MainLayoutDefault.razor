@using Student_Success_Planner.Data
@inherits LayoutComponentBase
@inject BreadcrumbService breadcrumbService
@*<div class="sidebar">
        <NavMenu />
    </div>*@

@inject NavigationManager  navigationManager

<div class="main">
    <div class="top-row px-4 auth">
        <Button Clicked="@GoStudent" Color="Color.Success"> View as Student </Button>
        <LoginDisplay />
        <a href="https://docs.microsoft.com/aspnet/" target="_blank">About</a>
    </div>

    <div class="content px-4">
        @Body
    </div>
</div>

@code{

    void GoStudent()
    { 
        breadcrumbService.AuthorizeSetter(true);
        try
        {
            navigationManager.NavigateTo( "/colleges/departments/programs/success-map");
        }
        catch (Exception e)
        {
            Console.WriteLine(e);
        }
    }
}